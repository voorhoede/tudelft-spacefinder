{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "space.json",
  "description": "A study space at TU Delft",
  "$ref": "#/definitions/space",
  "definitions": {
    "space": {
      "type":"object",
      "properties": {
        "slug": { "type": "string" },
        "name": { "type": "string" },
        "exchangeBuildingId": {
          "type": "string",
          "description": "Used to map a building to a calendar in exchange"
        },
        "exchangeRoomId": {
          "type": "string",
          "description": "Used to map a room to a calendar in exchange"
        },
        "buildingNumber": { "type":"integer" },
        "roomId": { "type": "string" },
        "floor": { "type": "string" },
        "seats": { "type": "integer" },
        "tables": { "type": "integer" },
        "latitude": { "type": "number", "description": "@TODO" },
        "longitude": { "type": "number", "description": "@TODO" },
        "openingHours": { "$ref": "base.json#/definitions/openingHours", "description": "@TODO" },
        "facilities": { "$ref": "#/definitions/facilities" },
        "i18n": {
          "type": "object",
          "properties": {
            "nl": {
              "$ref": "#/definitions/dictionary"
            },
            "en": {
              "$ref": "#/definitions/dictionary"
            }
          },
          "required": [
            "nl",
            "en"
          ],
          "additionalProperties": false
        }
      },
      "additionalProperties": false,
      "required": [
        "facilities",
        "floor",
        "latitude",
        "longitude",
        "name",
        "roomId",
        "seats",
        "slug",
        "tables"
      ]
    },
    "dictionary": {
      "type": "object",
      "properties": {
        "floor": { "type": "string" }
      }
    },
    "facilities": {
      "type": "object",
      "properties": {
        "adjustableChairs": { "type": "boolean" },
        "studyType": {
          "type": "string",
          "enum": ["self", "group"]
        },
        "quietness": {
          "type": "string",
          "enum": ["silent", "quiet", "noisy"]
        },
        "bookable": { "type": "boolean" },
        "daylit": { "type": "boolean" },
        "powerOutlets": { "type": "boolean" },
        "ethernet": { "type": "boolean" },
        "stationaryPC": { "type": "boolean" },
        "whiteBoard": { "type": "boolean" },
        "smartBoard": { "type": "boolean" },
        "presentationScreen": { "type": "boolean" },
        "nearCoffeeMachine": { "type": "boolean" },
        "nearPrinter": { "type": "boolean" },
        "nearBathroom": { "type": "boolean" },
        "claimedByGroup": { "type": "boolean" }
      },
      "additionalProperties": false,
      "required":[
        "adjustableChairs",
        "studyType",
        "quietness",
        "bookable",
        "daylit",
        "powerOutlets",
        "ethernet",
        "stationaryPC",
        "whiteBoard",
        "smartBoard",
        "presentationScreen",
        "nearCoffeeMachine",
        "nearPrinter",
        "nearBathroom",
        "claimedByGroup"
      ]
    }
  }
}
